To allow users to choose their profile pictures and edit them, we install:
It;s a python library that lets u crop, edit images. It's actually installed 
as a server that why we don't pip install it:
sudo apt-get update
sudo apt-get install imagemagick libmagickcore-dev


Also, in requirments file include library called 'wand' that allows you to use libmagick.


How do we store images in the database?
Then, we will store image as a timestamp. you don't store actual file in a database. you actually reference  
a pointer. We make this pointer to be a simple number and that number will be a timestamp as we generate

