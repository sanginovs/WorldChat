Mongo Restart:
rm -fr ~/data/mongod.lock
mongod -f mongod.conf
chmod 755 mongo_restart
./mongo_restart



Creating user model for our user
add mongo Engine to application.py:

from flask.ext.mongoengine import MongoEngine



db= MongoEngine()
 Inside def:
    db.init_app(app)
    
    
#testing the model from the shell

python manage.py shell

from user.models import User

#creating instance of user 
user=User(username="sher", password= 'test', email='jones@gmail.com', first_name='Sher', last_name='Sanginov')

#In order to save it 
user.save()
#In mongoDB, object id will always be unique


Mongo Commands:
mongo 
show dbs #shows databases
use database_name #switches to a database_name
show collections  --> system.indexes(built-in table for collections), user 

db.user.find() --> shows all the record inside user collection

Get index
db.user.getIndexes()


How do we know database using index to find a record:
db.user.find({'u': 'sher'})  .explain()   --> it says BTree
nscanned --> how many object it hit 



To delete everything:
db.user.drop()